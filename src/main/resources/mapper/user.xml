<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<mapper namespace="com.bj.house.mapper.UserMapper">
    <select id="selectUsers" resulType="user">
        select id,name,email,phone from user
    </select>

</mapper>
